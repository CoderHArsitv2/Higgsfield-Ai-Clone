# NOTE: .agent-logs/ is deliberately NOT ignored. It is part of the submission
# and ships with the repo. Do not add it here.

node_modules/
dist/
build/
.DS_Store
*.log
!.agent-logs/*
.env
.env.local
__pycache__/
*.pyc

# backend
backend/.media/
backend/bin/
backend/.env

# vercel -- `vercel pull` writes this at the repo root, next to the
# project's Root Directory rather than inside it.
.vercel/

# frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env*.local
frontend/.env
